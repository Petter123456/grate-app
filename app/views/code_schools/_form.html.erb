<%= form_with(model: code_school, local: true) do |form| %>
  <% if code_school.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(code_school.errors.count, "error") %> prohibited this code_school from being saved:</h2>

      <ul>
      <% code_school.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :code_school_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
